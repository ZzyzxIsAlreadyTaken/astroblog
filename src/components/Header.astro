---
const navItems = [
  { label: "Home", href: "/" },
  { label: "Posts", href: "/posts" },
  { label: "About", href: "/about" },
];
---

<header class="bg-stone-50 shadow">
  <nav class="container mx-auto px-4 py-6">
    <ul class="flex gap-6">
      {
        navItems.map((item) => (
          <li>
            <a href={item.href} class="text-stone-700 hover:text-stone-900">
              {item.label}
            </a>
          </li>
        ))
      }
    </ul>
  </nav>
</header>
